{
  "name": "sales-oracle-server",
  "version": "1.0.0",
  "description": "Sales Oracle — Claude AI proxy backend for subscription-gated access",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
